### Pin versions to match container base to avoid API conflicts
### Container provides: transformers==4.51.3, accelerate==1.6.0

accelerate==1.6.0
# bitsandbytes
# datasets
# torch
transformers==4.51.3
# flash-attn>=2.0.0  # For FlashAttention optimization